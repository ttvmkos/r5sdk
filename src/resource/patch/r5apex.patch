//////////////////////////////////////////////////////////
// Base game executable patch file; this executable
// contains all patches applied directly to the image.

/////////////////////////////
/////////////////////////////
////   Code adjustments  ////
/////////////////////////////
/////////////////////////////
// This patch adds support for loading the 'LUMP_PAKFILE' lump as a discrete file.
// The function originally loaded the BSP, and stored the header in a local variable.
// This means that we need to store the BSP header in the lump too, if we want to load
// the packfile lump separately. To avoid this, we hook this function in SDK code,
// and load the BSP header into the static 's_MapHeader' memory. The patch below makes
// the function read the static fields instead, and uses the descritprs in the header to
// read out the lump path passed in. A separate tool sets the 'fileofs' field to 0, so the
// packfile lump can be loaded without any additional patching.
0x382152: "cmp dword ptr ds:[0x00000001666EC6C0], 0x50534272" // s_MapHeader.ident
0x382162: "mov eax, dword ptr ds:[0x00000001666EC6C4]"        // s_MapHeader.version

0x382174: "cmp dword ptr ds:[0x00000001666EC954], 0x10"       // s_MapHeader.lumps[LUMP_PAKFILE].filelen
0x382181: "movsxd rcx, dword ptr ds:[0x00000001666EC950]"     // s_MapHeader.lumps[LUMP_PAKFILE].fileofs

0x382188: "mov rdx, rcx" // Move the operation done at 0x382181 (stored into RCX) into RDX; no need to index into it again.

// Due to the simplification of the operation above, 2 bytes had to be nopped.
0x38218B: "nop"
0x38218C: "nop"

0x38226E: "movsxd r8, dword ptr ds:[0x00000001666EC950]"      // s_MapHeader.lumps[LUMP_PAKFILE].fileofs
0x382275: "movsxd rdx, dword ptr ds:[0x00000001666EC954]"     // s_MapHeader.lumps[LUMP_PAKFILE].filelen

0x38247B: "cmp dword ptr ds:[0x00000001666EC6C0], 0x50534272" // s_MapHeader.ident
0x38248B: "mov eax, dword ptr ds:[0x00000001666EC6C4]"        // s_MapHeader.version

0x38249D: "cmp dword ptr ds:[0x00000001666EC954], 0x10"       // s_MapHeader.lumps[LUMP_PAKFILE].filelen
0x3824B6: "movsxd r8, dword ptr ds:[0x00000001666EC950]"      // s_MapHeader.lumps[LUMP_PAKFILE].fileofs

0x38257E: "movsxd r8, dword ptr ds:[0x00000001666EC950]"      // s_MapHeader.lumps[LUMP_PAKFILE].fileofs
0x382585: "movsxd rdx, dword ptr ds:[0x00000001666EC954]"     // s_MapHeader.lumps[LUMP_PAKFILE].filelen

// This patch increases the 'CModelRenderSystem::m_BoneToWorldOriented' memory buffer size to 2MiB. Previously,
// this was 1MiB, but would occasionally overflow when there are about 80 players rendering on the client's
// screen. This satisfies the required memory size to render all 120 player models (active players on server).
0xE6530: "mov     r8d, 200000h"

// This patch assigns the model and anim cache pointers to NULL instead of 0xDEADFEEDDEADFEED,
// if the asset failed to load. The 0xDEADFEEDDEADFEED (DC_INVALID_HANDLE) assignment was most
// likely done to catch errors in the asset loading system, since the pointer test won't fail
// while the pointer is invalid, this cause a hard crash. We however don't want this as this
// would cause issues loading BSP's with missing assets (the SDK handles the errors properly
// and also logs what was failed to load).
0x1E3CB6: "xor rax, rax" // NULL RAX instead of mov'ing '0xDEADFEEDDEADFEED' to cache ptr in 'Pak_UpdateModelAsset()'
0x1E3EE2: "xor rax, rax" // NULL RAX instead of mov'ing '0xDEADFEEDDEADFEED' to cache ptr in 'Pak_UpdateAnimRigAsset()'

// If we don't have an STBSP file, the field s_textureStreamMgr.hasResidentPages will be false. But if the texture
// stream manager is initialized and the stream mode is set to default, the code will force the stream mode to disabled.
// However, our engine has a working implementation of the GPU driven texture streaming system, and the correct thing
// to do here is to not force anything, since our SDK will enable the GPU driven texture streaming system if an STBSP
// file wasn't provided for the level. We turn the conditional jump to an unconditional jump here to skip this code.
0x3E0363: "jmp 0Fh"

// -- TextureStreamMgr_FillItems
0x3E97AF: mov rax, ds:[0x00000001635A7918] // Dereference pointer in old static array to access new static array.

// -- Pak_UpdateTextureAsset
// Previously, this LEA'd the address of 's_textureStreamMgr', and used the instructions below to offset into the
// 'streamingTextures' member. But now we dereference and load the address of our new array directly into r14.
0x3EC4DA: mov r14, ds:[0x00000001635A7918]

// The following instruction patches remove the immediate '0x388' which is the actual offset to 'streamingTextures'
// from the start of the struct 'TextureStreamMgr_s', since we store the address of our new array directly into r14
// from the lea -> mov patch above, so we don't need to add that on top of the address.
0x3EC551: mov rdx, qword ptr ds:[r14+rax*8]
0x3EC559: mov qword ptr ds:[r14+rax*8], r10
0x3EC56D: mov qword ptr ds:[r14+rax*8], rdx

// -- TextureStreamMgr_GetStreamOverlay
// Previously, this LEA'd the base address of our module, and used the instructions below to offset into the
// 'streamingTextures' member using an immediate. But now we load the address of our new array directly into r12.
0x3EA627: mov r12, qword ptr ds:[0x00000001635A7918]

// The following patch removes the immediate '0x235A7918' which is the actual offset to 'streamingTextures'
// from the base address of our module. Since we store the address of our new array directly into r12 from
// the lea -> mov patch above, so we don't need to add that on top of the address.
0x3EA632: mov r9, qword ptr ds:[r12+rax*8]

// -- TextureStreamMgr_AddOrDrop
0x3E9AFD: mov r15, qword ptr ds:[0x00000001635A7918] // Swap LEA module base with MOV 's_textureStreamMgr.streamingTextures'.
0x3E9B8A: mov r15, qword ptr ds:[0x00000001635A7918] // Same as above.

0x3E9B0B: mov r8, qword ptr ds:[r15+rax*8] // Remove immediate '0x235A7918'.
0x3E9D5B: mov rcx, qword ptr ds:[r15+rax*8]
0x3E9DC1: mov rcx, qword ptr ds:[r15+rax*8]
0x3E9E18: mov r15, qword ptr ds:[0x00000001635A7918] // Swap LEA module base with MOV 's_textureStreamMgr.streamingTextures'.
0x3E9E30: mov r11, qword ptr ds:[r15+rax*8]

// Dereference old array to get pointer to new array for the call to TextureStreamMgr_FillItems
0x3E9C6B: mov r9, ds:[0x00000001635A7918]
0x3E9CB0: mov rcx, qword ptr ds:[0x00000001635A7918]

// r15 originally contained the module base, but this now contains the address of the new array,
// the job_JT_RegisteredTypes array took advantage of the module base being in r15 and used that
// to obtain JobType_s::field_2c at given array index. Since there wasn't enough space left inside
// the current block, a jump had to be created to an empty section at the end of the .text segment
// to recreate this logic. The module base is stored in r13 which will be clobbered directly after
// the call to this function (TextureStreamMgr_AddOrDrop).
0x3E9C7B: jmp 0x0000000141295119
0x1294519: lea r13, ds:[0x0000000140000000]
0x1294520: mov eax, dword ptr ds:[rax+r13*1+0x27EE1E2C]
0x1294528: jmp 0x00000001403EA883

/////////////////////////////
/////////////////////////////
////    Code defects     ////
/////////////////////////////
/////////////////////////////
// This fixes an engine bug where '-forceborder' did not force the border over the application window.
0x249DEE: "or      byte ptr [rdi+14h], 3" --> "and     byte ptr [rdi+14h], 0FDh";

// Concat happened due to bug in engine; Valve forgot a comma separator in the array.
0x1477876:  'FIELD_INTERVALFIELD_MODELINDEX\x00\x00' --> 'FIELD_INTERVAL\x00FIELD_MODELINDEX\x00';
0x1318C00: 0x0000000000 --> 0x7792474101; // Add new entry in 'g_FieldTypes', this entry points to the 'FIELD_MODELINDEX' string we separated from 'FIELD_INTERVAL'.

// This fixes a bug where the help string of a ConVar is set as the conVar name, which prevents you from
// setting it in the console due to the precense of space characters.
0x115EAE: "lea rax, ds:[0x141516578]" // Change ConVar name assignment from helpstring to "player_vehicle"
0x115EBC: "lea rax, ds:[0x141459F20]" // Change ConVar help string assignment from null string to the helpstring.
// This operation has been inserted within the function, all instructions past this one
// have been shifted.
0x115ECA: "lea rax, ds:[0x141324120]" // Load NULL_STRING back into rax to assign to usage string.
// Path below is exactly the same as above, but at a different address as this particular convar has
// 2 dynamic initializers.
0x15B8AE: "lea rax, ds:[0x141516578]"
0x15B8BC: "lea rax, ds:[0x141459F20]"
0x15B8CA: "lea rax, ds:[0x141324120]"

// In 'CInput::JoyStickApplyMovement' an extraneous 'fmin' clamp is performed on the frame time. BinDiff revealed that this was no longer performed on
// the 'Season 9.1 Genesis' executable. Further testing revealed that patching out just this clamp fixes the controller view stick problems when usercmd's
// get dropped in CL_Move.
0x6FD0E1: "movaps  xmm0, xmm6" // Move frame time directly into the register that originally contained the clamped frame time.
0x6FD0EE: "nop (x4)" // Nop 'minss   xmm0, xmm6' (extraneous clamp).
0x6FD114: "nop (x3)" // Nop 'movaps  xmm6, xmm0' (extraneous move operation).

// If we have a partial install and are still downloading optional streaming files, the game would unload the current level VPK
// and reload all RPak files once we have fully downloaded all the optional files and disconnected from the server. However, on
// listen server builds, the unloading of the VPK and RPak's happen too early; it will unload the files while the server is still
// active. We should check on CHostState::m_bActiveGame and only unload the current level VPK and RPak's if this is false. The
// RPak reloading logic has been fixed in `Mod_RunPakJobFrame`, however the VPK code is inline and the check has to be embedded
// in the assembly here.
0x2969B7: jmp 0x00000001412951A8                      // jump to code cave.
0x12945A8: cmp byte ptr ds:[0x0000000141736384], 0x0  // Check if g_HostState.m_bActiveGame is false.
0x12945AF: jne 0x00000001402975C5                     // Jump to `Mod_RunPakJobFrame` call if condition isn't met, this new check skips `Mod_UnloadCurrentLevelVPK` as its still being used.
// Below the reconstruction of overwritten bytes caused by the long jump to code cave.
0x12945B5: cmp dword ptr ds:[0x000000014180A128], 0x2 // Checks if client signon is SIGNONSTATE_CONNECTED.
0x12945BC: jmp 0x00000001402975BE                     // jump back to original code (handles the SIGNONSTATE_CONNECTED check).

// ------------------------------------------------------------------------------------------------
// This fixes an engine bug causing alphasort (translucent world) textures to not stream when using
// the GPU driven feedback approach. Since we need to run and apply an update during R_DrawAlphaSort(),
// we need to create a separate set of DX buffers and handles to allow this to work without interfering
// with other render passes. For this, the static arrays had to be increased with the following set of
// patches bellow.
// The immediate to access 0x1671CAA80 has been increased with 4 bytes to create enough space
// for 3rd elem in int array at 0x1671CAA78, 0x1671CAA80 is now at 0x1671CAA84.
// *** sub_1403F4F80 ***
0x3F45EF: mov eax, dword ptr ds:[r13+r15*4+0x271CAA84]
0x3F482A: mov dword ptr ds:[r13+r15*4+0x271CAA84], eax

// *** sub_1403F4AA0 ***
0x3F4091: mov dword ptr ds:[0x00000001671CAA84], 0x3
0x3F409B: mov dword ptr ds:[0x00000001671CAA88], 0x3
0x3F40A5: mov dword ptr ds:[0x00000001671CAA8C], 0x3 // NEW: sets the 3rd elem
// all other instructions from here have been shifted to accomodate the new
// instruction at 0x3F40A6.
// ------------------------
// Since the increase of the i32 arrays above with 1 element each (8 bytes total). We need to
// shift this bool at 0x1671CAA88 with 8 bytes downwards as its originalspace has now been used.
// This is the only place this bool is ever accessed from. We have 24 bytes padding before we
// reach our next static element so enough space.
0x3F40B7: mov byte ptr ds:[0x00000001671CAA90], 0x1

// *** sub_1403F4A30 ***
0x3F3E84: mov dword ptr ds:[0x00000001671CAA84], 0x3
0x3F3E8E: mov dword ptr ds:[0x00000001671CAA88], 0x3
// Jump to code cave to set the 3rd (new) element, because there was not enough space left in
// the current function frame.
0x3F3E98: jmp 0x00000001412951E1
0x12945E1: mov dword ptr ds:[0x00000001671CAA8C], 0x3
0x12945EB: jmp 0x00000001403F4A2B // Jump back to top of the function frame
0x3F3E2B: add rsp, 0x28 // Restored instructions overwritten by long jmp to codecave.
0x3F3E2F: ret

// Since we increased dword_1671CAA48 and qword_1671CAA10 from 6 elements to 9 elements,
// we need to setup the last 3 elements here as well, so this immediate has been increased
// from 2 to 3 to take these into account.
0x3F3FA5: mov qword ptr ss:[rbp+0x7FF90], 0x3

// 1671CAA10 has been moved to address 1635A7940, because there was not enough room left in our
// old location to increase it from 6 elements to 9 to account for the alphasort render step.
// 1635A7940 is inside our old static texture streaming handle array that has been increased in
// commit ... -- since this large array is no longer used, we can use it for this change.
0x3F3F9E: lea rbx, ds:[0x00000001635A7940]
0x3F45D2: mov rdx, qword ptr ds:[r13+rbp*8+0x235A7940]
0x3F462C: mov rdx, qword ptr ds:[r13+rdi*8+0x235A7940]
0x3F4729: mov rdx, qword ptr ds:[r13+rdi*8+0x235A7940]

// 1671CAA48 has been moved to address 1635A7988, same reason as above, this array contains
// handles for the dx buffers.
0x3F3F97: lea rdi, ds:[0x00000001635A7988]
0x3F44F8: lea rsi, ds:[r13+0x235A7988]
// ------------------------------------------------------------------------------------------------

// ------------------------------------------------------------------------------------------------
// This fixes a regression that was caused after a refactor on HUD data structures; the base sizes
// are now written in a child structure, however, the function `Hud_SetScale` still accessed the old
// root structure members. This regression broke the HUD overlays for weapons such as the smart pistol,
// SRAM rocket launcher titan homing rockets, etc.. as well as the abstracted script function binding.
// The `Hud_SetScaleX()` and `Hud_SetScaleY()` methods actually do access the correct child data members
// confirming this function to be broken. The patch is simple; the child data structure is accessed
// below our incorrect instructions for obtaining the base size, we just move the instruction above the
// getters for the base size, and use that register instead with the updated offsets to obtain the right values.

// *** Hud_SetScale ***
0x9841DA: mov rcx, qword ptr ds:[r14+0x28]
0x9841DE: movd xmm0, dword ptr ds:[rcx+0x144]
0x9841E6: movd xmm1, dword ptr ds:[rcx+0x148]
// ------------------------------------------------------------------------------------------------

// ------------------------------------------------------------------------------------------------
// This fixes a crash if we run out of tracing slots, the array is of a fixed size and can run
// out of room in which the slot -1 is returned and the structure pointer is set to NULL. However,
// the structure pointer is still dereferenced leading to the crash. For the patch, the nulling of
// this register has been removed and the new space is used to transform the short jmp instruction
// to a long jmp to return gadget (which for all these functions either sets the slot result (-1)
// to the provided pointer, or into EAX).

// *** sub_1408AD8D0 ***
0x8ACD28: jmp 0x00000001408ADA0E // long jump to return gadget if slot = -1 (used to be short jmp to rest of code exec).

// *** sub_1405F2320 ***
0x5F1841: jmp 0x00000001405F252D

// *** sub_1405F2570 ***
0x5F1A7A: jmp 0x00000001405F2766

// *** sub_140608EE0 ***
0x608692: jmp 0x0000000140609370

// *** sub_14085D900 ***
0x85D57E: je 0x000000014085E22C

// *** sub_1408AD720 ***
0x8ACB78: jmp 0x00000001408AD8AE

// *** sub_140951D30 ***
0x95118D: jmp 0x0000000140951E8B

// *** sub_140954C60 ***
0x955206: jmp 0x0000000140955EC9
0x955309: jmp 0x0000000140955FD3

// *** sub_140A46ED0 ***
0xA46531: jmp 0x0000000140A4722E
// ------------------------------------------------------------------------------------------------

/////////////////////////////
/////////////////////////////
//// Exploitable defects ////
/////////////////////////////
/////////////////////////////
// This fixes a stack smash in 'CNetChan::ProcessSubChannelData' caused by the last fragment
// of a split packet, which could exceed the stack allocated buffer size of 560 bytes.
0x117F484: "jmp 0x1412950AE" // Jump to code cave (alignment padding at end of executable segment).
0x12944AE: "cmp rbp, 0x230"  // Check if fragment size of 'last' split packet doesn't exceed stack buffer size.
0x12944B5: "jg 0x140261CE6"  // Jump to gadget (xor al, al; pop..; ret;).
// Below the reconstruction of overwritten bytes caused by the long jump to code cave...
0x12944BB: "lea r8d, ds:[rbp*8]" // fragSize << 3
0x12944C3: "mov rcx, rbx"
0x12944C6: "jmp 0x140261A6B" // Jump back to original code; final split packet fragment is sane.

// This fixes a vulnerability in which the index field in the NET_SetConVar message
// could be used to read outside the static userinfo cvar string array.
0x030DD2B: "jmp 0x1412950E3" // Jump to code cave (alignment padding at end of executable segment).
// The instruction prior to the jump above performs a 'test' on the eax register.
// If the zero flag is set, it jumps to the rebuild of overwritten instructions
// caused by the long jmp to code cave.
0x12944E3: "je 0x1412950EE"
// This check was missing causing OOB reads! String array size is 0x27,
// so only index into it if its within bounds..
0x12944E5: "cmp eax, 0x28" 
0x12944E8: "jb 0x14030E951"
// The following is rebuild as the long jump overwrote it,
// basically perform what was overwritten and jump back to
// original code...
0x12944EE: "mov r8d, 0x104"
0x12944F4: "jmp 0x14030E933"

// This fixes a vulnerability in which the SQClass' '_members' buffer could overrun due to a missing check for current member count in the class.
// This has been patched on the Squirrel repo in commit: https://github.com/albertodemichelis/squirrel/commit/23a0620658714b996d20da3d4dd1a0dcf9b0bd98
0x1067595: "jmp 0x1412950F9"                  // Jump to code cave (alignment padding at end of executable segment).
0x12944F9: "mov rcx, qword ptr ds:[rbx+0x40]" // Load "SQClass::_members" into RCX (unused stack register in current compound block).
                                              // NOTE: 'this' ptr has been mov'd to a non-volatile register due to a function call in previous block.
0x12944FD: "mov eax, dword ptr ds:[rcx+0x54]" // Load "SQTable::_usednodes" into EAX (unused stack register in current compound block).
                                              // NOTE: inline 'SQTable::CountUsed()'
0x1294500: "cmp eax, 0xFFFFFF"                // Compare 'this->_members->_usednodes' against constant 'MEMBER_MAX_COUNT'.
0x1294505: "jge 0x141295112"                  // Short jump to FAIL block (if 'this->_members->_usednodes' equals or exceeds MEMBER_MAX_COUNT).
0x1294507: "mov ecx, dword ptr ds:[rsi]"      // Restored code overwritten by long jump patch at 0x1067595.
0x1294509: "mov rdi, qword ptr ss:[rbp-0x38]" // Restored code overwritten by long jump patch at 0x1067595.
0x129450D: "jmp 0x14106819B"                  // Jump to original code (if 'this->_members->_usednodes' is lower than MEMBER_MAX_COUNT).
0x1294512: "xor bl, bl"                       // Mark failure
0x1294514: "jmp 0x141068429"                  // Jump to return gadget which releases temporary handles, and loads BL into RAX to be returned.
